// react imports
import React, { Component } from 'react';

class Pins extends Component {
    render() {
        return (
            <div>hello from pins</div>
        );
    }
};

export default Pins;